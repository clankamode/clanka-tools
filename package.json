{
  "name": "clanka-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Clanka tooling — Discord worker, shared utilities",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dep-graph": "node scripts/dep-graph.mjs",
    "build": "cd workers/clanka-discord && npm run build",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.0"
  },
  "workspaces": [
    "workers/clanka-discord"
  ]
}
